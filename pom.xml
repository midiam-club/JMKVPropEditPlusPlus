<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>io.github.brunorex</groupId>
	<artifactId>jmkvpropedit</artifactId>
	<version>2.3.0</version>
	<name>JMkvpropedit</name>
	<description>A Java GUI for mkvpropedit</description>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>21</maven.compiler.source>
		<maven.compiler.target>21</maven.compiler.target>
		<maven.compiler.release>21</maven.compiler.release>

		<!-- Plugin versions -->
		<maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
		<maven-shade-plugin.version>3.6.0</maven-shade-plugin.version>
		<maven-surefire-plugin.version>3.5.3</maven-surefire-plugin.version>
		<exec-maven-plugin.version>3.5.0</exec-maven-plugin.version>
		<jsign-maven-plugin.version>6.0</jsign-maven-plugin.version>
		<junit-jupiter.version>5.12.2</junit-jupiter.version>
		<mockito.version>5.17.0</mockito.version>
		<spotbugs.version>4.9.3.0</spotbugs.version>
		<jacoco.version>0.8.13</jacoco.version>
		<dependency-check.version>12.1.2</dependency-check.version>

		<!--
		  Code signing settings (self-signed dev cert by default).
		  The keystore password and key password are read from environment
		  variables so they are never hard-coded in source control:
		    export CODESIGN_STOREPASS=your_store_password
		    export CODESIGN_KEYPASS=your_key_password
		  Or pass them as Maven properties:
		    mvn clean package -Dcodesign.storepass=yourpass -Dcodesign.keypass=yourpass
		  For production releases, replace with a real code-signing certificate.
		-->
		<codesign.keystore>${basedir}/build/codesign.jks</codesign.keystore>
		<codesign.alias>codesign</codesign.alias>
		<codesign.storepass>${env.CODESIGN_STOREPASS}</codesign.storepass>
		<codesign.keypass>${env.CODESIGN_KEYPASS}</codesign.keypass>
	</properties>

	<dependencies>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>2.18.0</version>
		</dependency>
		<dependency>
			<groupId>org.ini4j</groupId>
			<artifactId>ini4j</artifactId>
			<version>0.5.4</version>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-compress</artifactId>
			<version>1.27.1</version>
		</dependency>
		<dependency>
			<groupId>org.tukaani</groupId>
			<artifactId>xz</artifactId>
			<version>1.10</version>
		</dependency>
		<dependency>
			<groupId>com.formdev</groupId>
			<artifactId>flatlaf</artifactId>
			<version>3.5.4</version>
		</dependency>

		<!-- Logging: SLF4J API + Logback -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>2.0.18</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>1.5.18</version>
		</dependency>

		<!-- JUnit 5 (Jupiter) -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${junit-jupiter.version}</version>
			<scope>test</scope>
		</dependency>
		<!-- Vintage engine: allows running JUnit 4 tests during migration -->
		<dependency>
			<groupId>org.junit.vintage</groupId>
			<artifactId>junit-vintage-engine</artifactId>
			<version>${junit-jupiter.version}</version>
			<scope>test</scope>
		</dependency>
		<!-- Mockito for mocking external dependencies in tests -->
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven-compiler-plugin.version}</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven-surefire-plugin.version}</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>${maven-shade-plugin.version}</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<minimizeJar>false</minimizeJar>
							<transformers>
								<transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<mainClass>io.github.brunorex.JMkvpropedit</mainClass>
								</transformer>
								<transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
							</transformers>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
		</resources>
	</build>

	<profiles>
		<!-- Windows-only profile: generates .exe via launch4j -->
		<profile>
			<id>windows-exe</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>exec-maven-plugin</artifactId>
						<version>${exec-maven-plugin.version}</version>
						<executions>
							<execution>
								<id>create-exe</id>
								<phase>package</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>java</executable>
									<arguments>
										<argument>-jar</argument>
										<argument>${basedir}/launch4j/launch4j.jar</argument>
										<argument>${basedir}/launch4j/config/exe-standalone.xml</argument>
									</arguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- Code signing: activated automatically when build/codesign.jks exists.
		     Generate a self-signed cert with (replace <password> with your own):
		       keytool -genkeypair -alias codesign -keyalg RSA -keysize 2048 -validity 365
		         -keystore build/codesign.jks -storepass <password>
		         -dname "CN=JMkvpropedit, O=JMkvpropedit, C=ES"
		         -ext "ExtendedKeyUsage=codeSigning"
		     Then set the environment variables before building:
		       export CODESIGN_STOREPASS=<password>
		       export CODESIGN_KEYPASS=<password>
		     For production releases, replace with a real code signing certificate.
		-->
		<profile>
			<id>codesign</id>
			<activation>
				<file>
					<exists>build/codesign.jks</exists>
				</file>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>net.jsign</groupId>
						<artifactId>jsign-maven-plugin</artifactId>
						<version>${jsign-maven-plugin.version}</version>
						<executions>
							<execution>
								<id>sign-exe</id>
								<phase>package</phase>
								<goals>
									<goal>sign</goal>
								</goals>
								<configuration>
									<file>${basedir}/JMkvpropedit.exe</file>
									<keystore>${codesign.keystore}</keystore>
									<alias>${codesign.alias}</alias>
									<storepass>${codesign.storepass}</storepass>
									<keypass>${codesign.keypass}</keypass>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- Quality profile: static analysis and security checks (not run by default) -->
		<profile>
			<id>quality</id>
			<build>
				<plugins>
					<!-- JaCoCo: code coverage -->
					<plugin>
						<groupId>org.jacoco</groupId>
						<artifactId>jacoco-maven-plugin</artifactId>
						<version>${jacoco.version}</version>
						<executions>
							<execution>
								<id>prepare-agent</id>
								<goals>
									<goal>prepare-agent</goal>
								</goals>
							</execution>
							<execution>
								<id>report</id>
								<phase>verify</phase>
								<goals>
									<goal>report</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<!-- SpotBugs: static bug detection -->
					<plugin>
						<groupId>com.github.spotbugs</groupId>
						<artifactId>spotbugs-maven-plugin</artifactId>
						<version>${spotbugs.version}</version>
						<configuration>
							<spotbugsVersion>4.9.8</spotbugsVersion>
						</configuration>
						<executions>
							<execution>
								<id>spotbugs-check</id>
								<phase>verify</phase>
								<goals>
									<goal>check</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<!-- OWASP Dependency-Check: CVE scanning -->
					<plugin>
						<groupId>org.owasp</groupId>
						<artifactId>dependency-check-maven</artifactId>
						<version>${dependency-check.version}</version>
						<configuration>
							<failBuildOnCVSS>7</failBuildOnCVSS>
						</configuration>
						<executions>
							<execution>
								<goals>
									<goal>check</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
